{
  "name": "autogtm",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "check-types": "turbo run check-types",
    "test": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^25.0.1",
    "turbo": "^2.3.0",
    "vitest": "^2.1.8"
  },
  "packageManager": "npm@10.8.2"
}
